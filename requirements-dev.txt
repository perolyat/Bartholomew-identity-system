pytest
pytest-asyncio
pytest-mock
# Required by the coverage steps in .github/workflows/ci.yml (and by the
# legacy tests.yml). It was previously undeclared, so any `pytest --cov=...`
# invocation failed with "unrecognized arguments: --cov".
pytest-cov
ruff
black
pre-commit
mypy
httpx
freezegun
pytest-timeout
